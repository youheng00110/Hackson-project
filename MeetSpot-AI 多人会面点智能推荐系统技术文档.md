# MeetSpot-AI 多人会面点智能推荐系统技术文档

**产品访问地址**：[https://modelscope.cn/studios/fishdragon/MeetSpot-AI](https://modelscope.cn/studios/fishdragon/MeetSpot-AI)

**版本**：V1.0

**更新日期**：2026.03.14

**技术架构**：前后端分离 + AI决策引擎

**核心能力**：基于多维度数据的多人会面点智能推荐、AI自然语言决策辅助、实时路径规划与可视化

## 目录

1. [产品概述](#1-产品概述)

2. [使用方法与教程](#2-使用方法与教程)

3. [产品技术方案](#3-产品技术方案)

4. [商业模式](#4-商业模式)

5. [附录：术语表](#附录-术语表)

## 1 产品概述

### 1.1 产品定位

MeetSpot-AI 是一款面向“多人从不同地点出发，如何快速选定一个大家都方便的会面地点”的智能推荐系统。产品融合地图服务、多维度加权算法与 AI 辅助决策，把原本依赖经验、反复沟通、来回对比的选址流程，转化为可量化、可解释、可落地的推荐结果。

我们希望它解决的不只是“选哪家店/哪座商场”，而是一个更普遍的协作问题：

- 多人协同成本高：每个人的位置、出行方式、时间偏好不同，沟通容易陷入“各说各的近”。
- 决策缺少统一依据：没有共同的量化指标来平衡“公平性（差距小）”与“效率（整体快）”。
- 结果难以执行：即使选出一个点，也需要看路线、看是否可达、是否适配天气与场景。

MeetSpot-AI 的目标是：在尽可能少的输入成本下，让团队快速达成一致，并能直接执行出行。

### 1.2 核心解决痛点

- 信息不对称：参与方出发点分散，地理认知差异导致选址分歧；

- 决策成本高：传统沟通方式耗时久，多平台对比效率低；

- 时间协调复杂：不同出行方式（步行/骑行/驾车/公共交通）导致到达时间差异大，难以兼顾所有人。

### 1.3 项目意义与作用（价值主张）

从“产品作用”角度看，MeetSpot-AI 的价值不止是一个地图工具，而是一个轻量的多人决策系统：

1. **把沟通问题变成数据问题**：
  - 用“最大到达时差 / 时间方差 / 平均时长 / 总距离”等指标，替代主观争论。
  - 推荐结果包含路线信息与可解释指标，便于在群聊/会议中快速对齐并确定方案。

2. **提升决策效率与体验**：
  - 对用户来说，少量输入即可得到 Top 5 候选点，不必在多个平台之间切换对比。
  - 对组织来说，降低协调成本，尤其适用于团建、跨部门会议、外勤集合等高频场景。

3. **降低“选错地点”的隐性成本**：
  - 通过可达性校验与候选点过滤，减少“看起来近、实际难到/不适合”的情况。
  - 在天气与路况不佳时，AI 自动决策会给出更贴近日常的建议（室内商圈/近地铁/停车便利等）。

4. **支持跨设备、跨人群的协作方式**：
  - 单人也可快速算出结果；多人也可以通过房间码轻量组队，随时邀请他人参与决策。

#### 1.3.1 常见问题与说明（产品必要性与差异化）

本小节用于补充说明 MeetSpot-AI 的产品必要性、差异化定位及其与群聊协作、AI 助手（agent）之间的分工边界，帮助读者在阅读技术方案前快速建立一致认知。行文采用“问题 → 核心回答 → 举例/补充”的结构。

##### 常见问题 1：为什么要用我们的产品？

**核心回答**：多人会面地点选择存在天然的协作摩擦——讨论成本高、缺少统一量化依据、结果易产生不公平（某些人通勤时间明显更长）。MeetSpot-AI 通过算法计算与地图可视化，把“去哪见面”从主观讨论转为可执行的候选点方案（候选点 + 理由 + 路线）。

**举例**（3 人会面：A 在公司、B 在家、C 在学校）：

传统方式通常需要在群聊中反复提出地点并否决：

```text
A：去 XX 商场？
B：太远
C：去 YY？
A：太远
```

MeetSpot-AI 的流程更接近“提交信息 → 生成候选 → 快速达成一致”：

```text
输入三个人位置与出行方式
→ 计算更公平的会面区域/会面点
→ 输出若干候选场所与路线
→ 选择其一并直接执行出行
```

补充说明：系统目标是缩短决策链路，并用可解释指标（如最大到达时差、方差、平均时长等）减少分歧。

##### 常见问题 2：相对市面上已有产品（地图/点评）有什么优势？

**核心回答**：传统地图产品主要解决“单人到达某个目的地”的导航问题；MeetSpot-AI 聚焦“多人共同决策目的地”的协作问题，核心差异在于公平性计算、候选点生成与多人可执行方案整合。

| 能力 | 普通地图 | MeetSpot-AI |
| --- | --- | --- |
| 单人路线规划 | ✅ | ✅ |
| 多人公平地点计算 | ❌ | ✅ |
| 候选会面点生成与排序（可解释指标） | ❌ | ✅ |
| 附近场所（POI）推荐并与路线联动展示 | ❌ | ✅ |
| 协作入口（房间码/共享） | ❌ | ✅ |

##### 常见问题 3：为什么不直接在群里讨论？

**核心回答**：群聊可用于沟通，但难以稳定地产生“对所有人都可接受”的结论，常见问题是信息流混乱、缺少统一评估标准、决策时间不可控。MeetSpot-AI 的价值是把讨论收敛为结构化输入与可视化输出，降低协作成本。

**补充**：

- 信息混乱（多人同时给出地点，缺少收敛机制）：

```text
A：去 A 店
B：去 B 店
C：去 C 店
```

- 不公平风险：最终地点可能对个别成员通勤成本显著偏高
- 讨论成本：需要跨平台核对距离、路线、是否可达等信息

**产品化流程（示意）**：

```text
创建会面计划/房间
→ 分享链接或房间码
→ 成员补充位置与偏好
→ 系统生成候选点与路线
→ 选择并执行
```

##### 常见问题 4：如何降低使用门槛并形成传播？

**核心回答**：优先覆盖高频社交/协作场景，通过低门槛流程提升首次使用转化，并利用链接/房间码在群聊场景的天然传播完成扩散。

- 高频场景：朋友聚会、同学聚餐、同事聚餐、旅行集合、临时集合等
- 低门槛：尽量减少注册与学习成本（以“分享 → 填写 → 出结果”的短路径为主）
- 社交传播：发起人将链接/房间码分享至群聊，参与者进入即完成协作；传播效果受场景与人群结构影响，需结合实际运营数据验证

##### 常见问题 5：为什么不直接用 AI agent 完成？

**核心回答**：在“多人会面点选择”这一交互密集的场景中，agent 更适合作为辅助（偏好解析、推荐解释、策略建议），而不是替代完整体验。原因在于：该场景需要结构化数据收集、地图可视化、路径计算、POI 检索与多人协作状态的连续交互，纯文本对话往往需要多轮补充信息且难以落地执行。

**对比说明**：

- agent 往往需要反复追问：所有人地址、出行方式、约束条件、地点类型等
- 输出多为文本建议（例如“建议在 XX 区域见面”），用户仍需自行打开地图核对路线与场所
- MeetSpot-AI 将路线、候选点与可解释指标集成为“可执行方案”，减少二次操作

##### 常见问题 6：一句话概述

> 传统地图主要解决“一个人如何到达某个地点”，而现实中更常见的协作问题是“多人如何快速找到一个相对公平、可执行的会面地点”。MeetSpot-AI 通过算法计算多人通行成本并生成候选会面点，结合场所推荐与路线规划，把群聊讨论转化为可落地的决策结果，从而降低协调成本并提升会面效率。

**项目一句话定位**：我们解决的是多人见面地点选择困难的问题。

### 1.4 房间码组队（轻量化协作）的优势

“房间码组队”是我们在产品交互层面强调的协作入口：用户通过一个 6 位房间码即可创建/加入协作房间，把分散的参与者快速聚合到同一个决策上下文里。

它的核心好处是“轻量、方便、低门槛”：

- **无需复杂注册/登录**：对临时聚会、团建集合非常友好；发起人创建房间后分享房间码即可。
- **跨终端自然协作**：每个人用自己的手机/电脑打开页面，输入房间码加入，同步进入同一场会面规划。
- **组织成本低**：房间码天然适合在微信群/钉钉/飞书等场景传播，操作路径短。
- **隐私与临时性更符合场景**：用户只需提供本次会面的位置信息，不必沉淀长期个人数据。

> 说明：当前版本前端已提供“创建房间/加入房间”的交互入口与房间码校验规则；多人实时同步与房间状态管理可在后续按“技术迭代规划”接入 WebSocket/后端房间服务。

### 1.5 核心技术亮点

- 前后端分离架构，模块化开发，便于扩展与维护；

- 加权中心点+多维度评分算法，最小化到达时间差异；

- 通义千问（DashScope 兼容接口）赋能的AI决策助手，实现多源数据的自然语言解读；

- 百度地图API深度集成，支持实时路况、路径规划、POI精准搜索；

- 多页面（Hash 路由）交互：首页、单终端模式、房间码多终端模式（开发中）；
- AI 对话输入：自然语言解析会面类型/半径/策略/出行方式并触发搜索。

### 1.6 典型应用场景

- 朋友聚会：多人从不同商圈出发，快速选一个“差不多都方便”的咖啡/餐厅/商场。
- 家庭聚餐：兼顾老人/小孩出行方式与耗时，优先选择地铁便利或停车友好的地点。
- 企业团建/商务会议：跨部门成员分散，缩短协调时间，提升会议准点率。
- 临时集合与户外活动：在天气变化、路况波动时更需要一个“可执行”的会面点方案。

## 2 使用方法与教程

本节以“用户视角”给出从进入系统到完成一次会面点推荐的完整路径，并提供截图与视频接口占位。

### 2.1 进入产品

- 线上访问：使用产品访问地址进入系统。
- 本地运行：参考第 3 章“产品技术方案”中的“部署与运行要求”小节。

### 2.2 单终端模式（推荐流程）

适用：一个人集中输入所有参与者信息（最常见、最快的方式）。

1. 在侧边栏进入“单终端模式”。
2. 添加参与者：填写姓名、出发地点（支持联想搜索），并选择每个人的出行方式。
3. 向 AI 助手描述你的会面需求（例如“想在某处附近见面”“下雨天优先室内”“要离地铁近”）。

![图1：单终端模式下向 AI 助手提出会面偏好](1.png)

4. AI 自动分析并给出推荐会面点（含理由/偏好匹配说明）。

![图2：单终端模式下 AI 推荐会面点示意](2.png)

5. 确认会面类型与搜索半径（默认 3000 米；场所稀疏时系统会自动扩大半径重试，最大 10000 米）。
6. 点击“寻找会面点”，系统生成候选点并进行路径规划。
7. 在地图上查看：
  - 参与者起点标记、会面点标记
  - 不同交通方式对应不同颜色的路线

![图3：单终端模式下的路径规划示意](3.png)

8. 需要微调时：可以拖拽人员或选中会面点的标记进行位置调整。

### 2.3 视频演示（占位接口）

> 将下面的 `src` 替换为线上视频链接或本地文件路径（例如 `demo.mp4`）。

```html
<!-- TODO: 替换为实际视频地址/文件路径 -->
<video src="REPLACE_WITH_VIDEO_URL_OR_PATH" controls style="max-width: 100%;"></video>
```

## 3 产品技术方案

本章聚焦“系统怎么实现”：包含整体架构、算法、AI、接口、第三方集成、性能与部署等关键技术点。

### 核心技术架构

本系统采用**前后端分离**的架构设计，遵循RESTful API规范，实现前端交互与后端业务逻辑解耦，同时集成第三方地图服务与AI大模型能力，整体架构分为前端展示层、后端服务层、算法引擎层、第三方服务层四层。

#### 前端技术栈

|技术组件|版本|核心作用|
|---|---|---|
|React|19.2.x|现代化 UI 框架，实现组件化界面|
|Vite|7.2.x|开发/构建工具|
|Axios|1.13.x|前端请求后端 API|
|Font Awesome|7.x|图标体系（侧边栏、按钮）|
|百度地图 JavaScript API|v3.0|地图可视化、标记、路线绘制、地点检索与定位|
|Hash 路由|自研（无 react-router）|多页面路由：home/single-terminal/multi-terminal/developing|
|CSS3|项目内样式文件|组件样式与响应式布局|

> 备注：前端服务封装文件命名为 `amapService.js`，但实际加载与调用的是百度地图 SDK（`BMap`）。该命名沿袭历史原因，文档以下统一称“百度地图 SDK/服务”。

#### 后端技术栈

|技术组件|版本|核心作用|
|---|---|---|
|Node.js|18+|高性能JavaScript运行时，支撑后端服务高并发|
|Express|5.2.x|Web 应用框架，提供 JSON API|
|Axios|1.13.x|调用百度地图 Web API、DashScope 兼容接口|
|dotenv|17.x|加载环境变量|
|cors|2.8.x|跨域访问|
|百度地图 Web 服务 API|按接口|地理编码/逆地理编码、路径规划、POI 搜索、天气、路况|
|DashScope（通义千问）兼容接口|按模型|`/chat/completions` 生成推荐说明（默认 `qwen-plus`）|

#### 架构核心特点

1. **职责清晰**：前端负责交互与可视化，后端负责业务逻辑、算法计算与第三方API调用；

2. **标准化接口**：所有前后端交互遵循RESTful API规范，支持跨端、跨平台调用；

3. **模块化开发**：前端组件化、后端服务化，单个模块迭代不影响整体系统运行；

4. **第三方服务解耦**：地图、AI大模型等能力通过接口集成，便于替换与扩展；

5. **可扩展性强**：支持快速接入新的出行方式、场景类型与数据源。

### 核心算法设计

系统核心推荐能力基于**加权中心点算法**与**多维度评分机制**实现，通过对时间、距离等核心维度赋予动态权重，计算出最优会面点，同时结合场景需求实现策略化调整。

#### 加权中心点算法（按出行速度加权）

当前实现中，“加权中心点”用于生成候选点的搜索中心，权重由参与者的**出行速度**决定：速度越慢的人权重越高（更偏向慢速出行方）。

设第 $i$ 个参与者位置为 $(lng_i, lat_i)$，其权重 $w_i = \frac{1}{speed_i}$，则：

$$
lng_{center}=\frac{\sum lng_i\cdot w_i}{\sum w_i},\quad lat_{center}=\frac{\sum lat_i\cdot w_i}{\sum w_i}
$$

速度映射（km/h）：步行 5、骑行 15、公共交通 25、驾车 40。

#### 多维度评分机制

在候选点生成后，系统会为每个候选点计算“每位参与者 -> 候选点”的路线信息（时长、距离、到达时间、polyline）。如有任意参与者无法计算路线，则该候选点会被丢弃，确保推荐结果可达。

当 `objective=balanced` 时，候选点综合评分由以下指标组成（分数越低越优）：

1. 时长方差（`timeVariance`）：参与者通行时长的方差（用于衡量离散度）
2. 最大到达时差（`maxTimeDiff`）：最晚到达与最早到达的差值（秒）
3. 平均通行时长（`avgDuration`）：参与者通行时长平均值（秒）
4. 总通行距离（`totalDistance`）：参与者通行距离求和（米）

综合评分公式：

$$
score=\frac{timeVariance}{10000}\cdot0.4+\frac{maxTimeDiff}{60}\cdot0.3+\frac{avgDuration}{60}\cdot0.2+\frac{totalDistance}{1000}\cdot0.1
$$

额外规则（用于提升可执行性）：

- 过滤政务/机关类 POI（避免推荐不可用地点）
- 大型商场名称加分（分数折扣，优先级提升）
- 小型零售/便利店降权（分数惩罚，避免体验不佳）

#### 动态策略适配

系统支持3种计算策略，算法可根据用户选择动态调整权重与评分规则：

1. **默认推荐（综合，objective=balanced）**：采用综合评分（时间方差/最大到达时差/平均时长/总距离），按分数从低到高排序；

2. **相对时差最小优先（objective=time_gap）**：最小化参与方“通行时长相对差值”$\frac{maxDuration-minDuration}{maxDuration}$，更强调到达时间一致性；

3. **相对距离差最小优先（objective=distance_gap）**：最小化参与方“通行距离相对差值”$\frac{maxDistance-minDistance}{maxDistance}$，更强调路程公平性。

### 功能模块技术实现

系统核心功能模块围绕**多人协同、智能推荐、可视化、AI决策**四大核心能力设计，各模块独立开发且通过接口实现数据互通，以下为核心模块的技术实现细节。

#### 多人协同输入模块

- **核心能力**：支持无限人数同时输入出发地点、选择出行方式，系统自动解析地理坐标；

- **技术实现**：

    1. 基于百度地图API实现**地址模糊搜索与智能联想**，提升地址输入准确性；

    2. 前端采用表单组件化设计，支持动态添加/删除人员信息，实时校验输入格式；

    3. 后端接收人员信息后，通过百度地图 Web API 将地址转换为经纬度（地理编码）；如需进一步降本增效，可在此处增加缓存（例如内存 LRU 或 Redis）；

    4. 支持百度地图 SDK 的浏览器定位（`BMap.Geolocation`）获取当前坐标，用于快速填入起点/拖拽微调；城市字段用于后端路径规划参数。

#### 出行方式适配模块

- **核心能力**：支持步行、骑行、驾车、公共交通4种出行方式，精准计算不同方式的通行时间与路径；

- **技术实现**：

    1. 对接百度地图Web API的**路径规划接口**，根据出行方式传入不同参数，获取精准的通行时间、距离与路径坐标；

    2. 前端根据路径坐标，通过百度地图JS API实现**不同颜色的路径渲染**，区分不同参与方的路线；

    3. 路径规划时长以第三方路径规划接口返回为准；AI 自动决策会额外拉取周边路况用于解释与排序（不直接回写路径规划时长）。

#### 智能推荐模块

- **核心能力**：基于加权算法与多维度评分，生成Top-N会面点推荐列表，展示核心数据（最大时差、平均用时、总距离）；

- **技术实现**：

    1. 后端根据人员坐标与出行方式，调用核心算法计算加权中心点；

    2. 以加权中心点为圆心，根据用户设置的**搜索半径**（默认3000米），调用百度地图POI搜索接口获取周边符合类型的商户；

    3. 对POI列表进行多维度评分，按评分从低到高排序（分数越低越优），生成推荐列表；

    4. 前端以卡片形式展示推荐结果，高亮核心数据（最大时差、平均用时、总距离）。

#### 地图可视化模块

- **核心能力**：直观展示参与方出发点、会面点位置、各参与方路径，支持地图缩放、拖拽、标记查看；

- **技术实现**：

    1. 基于百度地图JS API初始化地图实例，设置中心点为加权中心点；

    2. 对每个参与方的出发点与会面点进行**自定义标记**，区分不同角色；

    3. 通过路径坐标数组，实现**实时路径绘制**，支持路径高亮与详情查看；

    4. 响应式设计，适配电脑、平板、手机等不同设备的地图展示尺寸。

#### 类型筛选与半径调节模块

- **核心能力**：支持按会面类型筛选（餐厅/咖啡馆/公园/商场/图书馆等），自定义搜索半径；

- **技术实现**：

    1. 前端支持会面点类型选择与搜索半径调节（默认 3000 米）；当周边场所不足时可自动扩大半径重试（最大 10000 米）；

    2. 后端将类型与半径作为参数，传入百度地图POI搜索接口，实现精准筛选；

    3. 支持多类型同时选择，后端对多类型POI进行合并与去重处理。

#### AI决策助手模块

- **核心能力**：提供两种 AI 辅助方式：

  1. **AI 决策（大模型生成）**：将候选点与核心指标封装为 prompt，调用通义千问（DashScope 兼容接口）生成 Top5 推荐理由与总结。

  2. **AI 自动决策（规则 + 实时数据）**：后端自动拉取百度天气与周边路况，结合候选点名称特征（商场/地铁/停车/连锁口碑）与通行时间计算评分，输出结构化排序与理由。

- **技术实现**：

  - `/api/ai/decision`：需要 `DASHSCOPE_API_KEY`，默认模型 `qwen-plus`，请求体为 `input` 字符串。
  - `/api/ai/auto-decision`：使用 `BMAP_SERVER_KEY` 调用百度天气/路况接口，并对候选点进行打分排序。

### AI决策助手技术原理

AI决策助手是系统的核心智能引擎，基于**通义千问大模型**实现，通过**多源数据融合、精准场景匹配、自然语言生成**三大核心能力，将复杂的算法数据转化为易懂的推荐说明，降低用户认知负担。

#### 核心数据输入

AI决策助手的输入数据为多源融合的结构化数据，包括：

1. 交通数据：各推荐点的通行时间、实时路况、拥堵程度、公共交通覆盖度；

2. 环境数据：当前天气、温度，适配的会面点属性（室内/室外）；

3. 商户数据：商户评分、口碑、品牌类型、附加属性（停车便利/地铁直达）；

4. 算法数据：推荐点的评分、排序、与加权中心点的距离。

#### AI决策流程

1. **路况分析**：优先通过百度地图周边路况接口获取拥堵等级；如失败则根据“总距离/总耗时”估算平均速度并映射为拥堵等级；

2. **天气适配**：结合第三方天气API数据，根据天气情况推荐适配的会面点（如下雨天推荐室内商圈，晴天推荐公园/户外）；

3. **口碑评估**：对商户的评分、品牌稳定性进行筛选，排除低评分、高差评的商户；

4. **自然语言生成**：在“AI 决策”模式下，将上述分析结果封装为标准化 prompt，调用 DashScope 兼容接口的通义千问模型生成自然语言推荐；在“AI 自动决策”模式下直接输出结构化排序与理由；

5. **结果展示**：前端以卡片形式结构化展示推荐说明，高亮关键信息（如通行时间、地铁线路、商户评分）。

#### 输出规范

AI推荐说明采用 **“推荐方案+核心理由+专业建议”** 的结构，示例如下：

推荐方案：建议选择"万达广场(成都青羊店)"作为会面点。

核心理由：该位置距离各位出发点的总通行时间约10分钟，周边为中度拥堵；多云天气适配室内区域；为知名连锁综合体，体验更稳定。

专业建议：综合路况与天气，该点位通行时间更短且周边拥堵更低，天气不佳时对所有参与方更友好。

### 前后端接口规范

系统前后端交互采用 JSON over HTTP，主要为 POST（提交数据/触发计算）与少量 GET（健康检查）。当前代码实现的统一接口前缀为`/api`。

#### 通用请求头

|字段名|必选|类型|说明|
|---|---|---|---|
|Content-Type|是|string|固定为 application/json|

#### 通用响应格式

成功：

```json
{
  "success": true,
  "data": {}
}
```

失败：

```json
{
  "success": false,
  "error": "错误信息"
}
```

#### 核心接口清单

##### 健康检查

- **接口地址**：`GET /health`

##### 地理编码（地址转坐标）

- **接口地址**：`POST /api/geocode`

请求：

```json
{
  "address": "成都市青羊区日月大道一段978号",
  "city": "成都市"
}
```

响应：

```json
{
  "success": true,
  "data": { "lng": 103.9852, "lat": 30.6687 }
}
```

##### 逆地理编码（坐标转地址）

- **接口地址**：`POST /api/reverse-geocode`

```json
{
  "lng": 116.397428,
  "lat": 39.90923
}
```

##### POI 周边搜索

- **接口地址**：`POST /api/poi/search`

```json
{
  "location": "116.397428,39.90923",
  "keywords": "咖啡",
  "radius": 1000,
  "offset": 20
}
```

##### 会面点计算（核心推荐）

- **接口地址**：`POST /api/meeting-point/calculate`

```json
{
  "persons": [
    {
      "id": "person_1",
      "name": "用户1",
      "lng": 116.397428,
      "lat": 39.90923,
      "transportMode": "driving",
      "departureTime": 1710000000000,
      "city": "北京"
    },
    {
      "id": "person_2",
      "name": "用户2",
      "lng": 116.40,
      "lat": 39.90,
      "transportMode": "transit",
      "city": "北京"
    }
  ],
  "options": {
    "poiTypes": ["咖啡", "餐饮服务", "购物服务"],
    "searchRadius": 3000,
    "maxCandidates": 30,
    "city": "北京",
    "objective": "balanced"
  }
}
```

`objective` 可选值：

- `balanced`：综合评分（时间方差/最大时差/平均时长/总距离）
- `time_gap`：相对时差最小优先（使用时长相对差）
- `distance_gap`：相对距离差最小优先（使用距离相对差）

##### AI 决策（大模型）

- **接口地址**：`POST /api/ai/decision`

```json
{ "input": "请在Top5里选一个最适合下雨天、离地铁近的商场会面点，并给出理由" }
```

##### AI 自动决策（规则 + 实时数据）

- **接口地址**：`POST /api/ai/auto-decision`

```json
{
  "city": "北京",
  "districtId": "222405",
  "participants": [
    { "name": "用户1", "lng": 116.397428, "lat": 39.90923, "transportMode": "driving" }
  ],
  "candidates": [
    { "name": "万达广场", "lng": 116.40, "lat": 39.90, "city": "北京" }
  ]
}
```

### 第三方API集成

系统的核心能力依赖第三方API的集成，所有第三方API均通过**接口封装**实现调用，便于统一管理与替换，目前集成的核心第三方API如下：

#### 百度地图API（核心）

- **集成类型**：Web API（后端）、JS API（前端）；

**核心接口**（后端 Web 服务）：

- 地理编码：`/geocoding/v3/`
- 逆地理编码：`/reverse_geocoding/v3/`
- 路径规划：`/directionlite/v1/{mode}`（driving/walking/riding/transit）
- POI 搜索：`/place/v2/search`
- 天气：`/weather/v1/`
- 周边路况：`/traffic/v1/around`

- **调用方式**：后端通过HTTP请求调用Web API，前端通过JS脚本引入调用JS API；

- **鉴权方式**：API Key鉴权，前后端使用不同的API Key，限制调用权限；

**鉴权与配置**：

- 后端使用 `BMAP_SERVER_KEY`
- 前端使用 `VITE_BMAP_WEB_KEY`

> 现状说明：当前代码未内置“本地缓存/限流/重试”中间件；相关优化建议见下文“性能优化方案”与“可靠性与容错建议”小节。

#### 通义千问API

**集成类型**：DashScope 兼容接口（后端 `POST /chat/completions`）；

- **核心能力**：自然语言生成、多维度数据解读；

- **调用方式**：后端将结构化数据封装为prompt，通过HTTP POST请求调用；

**关键环境变量**：

- `DASHSCOPE_API_KEY`（启用 `/api/ai/decision` 时必填）
- `DASHSCOPE_API_BASE_URL`（可选，默认 `https://dashscope.aliyuncs.com/compatible-mode/v1`）
- `AI_MODEL`（可选，默认 `qwen-plus`）

#### 天气API

- **集成类型**：RESTful API（后端）；

- **核心能力**：获取当前城市的天气、温度、降水概率；

**调用方式**：后端基于百度天气接口，使用 `district_id` 获取天气。默认可从 `BMAP_WEATHER_DISTRICT_ID` 读取；也可由前端在 `/api/ai/auto-decision` 中显式传 `districtId`。

- **应用场景**：AI决策助手的天气适配分析，会面点类型的智能推荐。

### 性能优化方案

本章分为“已实现（代码现状）”与“建议优化（规划）”，避免将规划项误写为已落地能力。

#### 前端性能优化

1. **按需渲染覆盖物**：地图覆盖物在数据变化时统一清理并重绘，避免历史残留；

2. **地图渲染优化**：采用百度地图JS API的**增量渲染**方式，仅在数据变化时更新地图标记与路径；

3. **静态资源缓存**：由部署侧（Nginx/CDN）配置缓存策略；前端本身不强依赖；

4. **响应式渲染**：根据设备性能与屏幕尺寸，动态调整地图的渲染精度。

#### 后端性能优化

已实现：

- Node.js 异步 I/O + 串行计算路线（降低对第三方 API 的并发压力）
- 统一错误处理中间件输出 `success/error`

建议优化（未内置）：

- 为地理编码/POI/路径规划增加缓存（内存 LRU 或 Redis），降低重复调用
- 为第三方 API 调用增加限流与熔断，避免触发配额与雪崩
- 在网关或反向代理层开启 gzip/br 压缩

#### 第三方API调用优化

现状：

- 会面点评分阶段对每个人的路径规划采用串行 + 间隔延迟，避免瞬时并发过高
- 公共交通模式在“公交/地铁失败”时，会尝试火车/飞机的基于直线距离估算（降级）

建议：

- 引入重试（带指数退避）与超时区分（网络/配额/参数）
- 对天气/路况接口失败时继续输出“未知天气/估算拥堵”并在前端提示

#### 性能指标

性能表现会受第三方 API 配额、城市规模、候选点数量与人数影响。建议在部署环境通过压测与真实 Key 评估，并以 `/api/meeting-point/calculate` 返回的 `calculationTime` 字段做线上观测。

### 部署与运行要求

#### 前端部署要求

**运行环境**：Node.js 16+、npm；

- **构建命令**：`vite build`，生成的静态文件可部署至Nginx/Apache/CDN；

- **部署要求**：支持HTTPS（百度地图API、AI API要求）；

- **适配环境**：主流浏览器（Chrome/Firefox/Edge/Safari），版本≥90。

#### 后端部署要求

**运行环境**：Node.js 18+；

- **依赖安装**：`npm install`安装package.json中的所有依赖；

**运行命令**：`npm start`（等价于 `node src/app.js`）；生产环境可用 PM2：`pm2 start src/app.js`；

**端口配置**：默认端口 3001（`PORT` 可覆盖）；

**关键环境变量（后端）**：

```env
# 必填：百度地图服务端 Key
BMAP_SERVER_KEY=your_key

# 可选：服务端端口
PORT=3001

# 可选：百度天气 district_id（用于 AI 自动决策默认天气城市）
BMAP_WEATHER_DISTRICT_ID=222405

# 可选：如启用 /api/ai/decision（大模型生成）则必填
DASHSCOPE_API_KEY=your_key
DASHSCOPE_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
AI_MODEL=qwen-plus
```

**关键环境变量（前端）**：

```env
VITE_BMAP_WEB_KEY=your_key
VITE_API_URL=http://localhost:3001/api
```

#### 产品线上部署地址

**MeetSpot-AI在线体验**：[https://modelscope.cn/studios/fishdragon/MeetSpot-AI](https://modelscope.cn/studios/fishdragon/MeetSpot-AI)

### 未来技术迭代规划

基于当前技术架构，系统将围绕**实时协同、AI算法优化、多场景拓展、数据源丰富**四大方向进行技术迭代，提升系统的智能性、协同性与可扩展性。

#### 多人实时协同模式

- **核心思路（规划）**：引入 WebSocket（或等价实时通道）实现位置与状态的低延迟同步，并以**房间码**作为轻量的组队/权限边界。

- **预期体验（规划）**：用户输入房间码进入专属会面房间，各自选择/上报位置后，系统可按成员实时变化自动刷新候选会面点与路线结果，减少重复输入与沟通成本。

- **当前实现状态（对齐代码）**：前端已提供“创建房间/加入房间”的页面入口与房间码校验，但创建/加入与实时同步的后端能力尚未实现，因此本文将其作为后续迭代方向描述。

#### AI算法优化

1. 引入**机器学习模型**，基于用户的使用行为（如选择的会面点、出行方式、场景类型）实现个性化推荐，打造“千人千面”的推荐体验；

2. 优化多维度评分机制，加入**用户偏好权重**，支持用户自定义评分维度（如优先考虑停车便利、优先考虑性价比）；

3. 实现**AI策略自学习**，根据用户的选择反馈，动态调整算法权重与推荐规则。

#### 数据源丰富与拓展

1. 接入更多地图服务：支持高德地图、腾讯地图等多地图API切换，提升地域覆盖度；

2. 整合实时交通数据：接入实时公交、共享单车、网约车等数据源，丰富出行方式与通行时间计算；

3. 拓展商户数据：接入大众点评、美团等商户平台的评分、优惠、营业状态数据，提升推荐的丰富性。

#### 技术架构升级

1. 后端引入**微服务架构**，将核心算法、第三方API调用、用户管理拆分为独立微服务，提升系统的可扩展性与容错性；

2. 引入**分布式缓存**（Redis），替代本地缓存，提升缓存的共享性与有效性；

3. 支持**容器化部署**（Docker）与**容器编排**（K8s），提升部署效率与集群管理能力。

#### 多端适配拓展

1. 开发微信小程序、支付宝小程序，实现免安装的轻量化体验；

2. 开发移动端APP（iOS/Android），支持更丰富的原生交互（如GPS实时定位、推送通知）；

3. 适配企业微信、钉钉等办公平台，为B端商务会议、团建场景提供专属插件。

## 4 商业模式

本章从“怎么把产品做成可持续的业务”出发，给出可落地的商业化路径与收入结构。

### 4.1 目标客户与细分场景

- **C 端**：朋友聚会、家庭聚餐、跨城会合、临时集合。
- **B 端**：企业团建与外勤集合、跨部门会议/访客接待、连锁门店选址与活动集合、园区/商圈的导流与运营。

### 4.2 核心价值主张

1. **降本增效**：减少沟通轮次与选址时间，把“拍板”从主观争论变为数据与可解释推荐。
2. **可执行**：路线可达性校验 + 地图可视化，使推荐结果可直接用于出行。
3. **轻量协作**：房间码组队降低协作门槛，适合在群聊中快速传播与召集。

### 4.3 收入来源（变现方式）

- **订阅制（SaaS）**：
  - 团队版/企业版按席位或按月/年订阅，提供房间管理、历史记录、模板场景、企业配置等。
- **按量计费（API/调用量）**：
  - 对外提供“会面点推荐 API/AI 决策 API”，按请求量、并发、SLA 分档收费。
- **B 端定制与私有化部署**：
  - 为企业/园区提供私有化部署、定制算法权重、内部地图 Key/安全合规方案等。
- **渠道与生态分成**：
  - 与商圈/连锁品牌/活动平台合作，推荐结果中提供可选的“到店权益/预约入口”，按转化分成（需确保合规与用户体验）。

### 4.4 成本结构与关键资源

- **第三方调用成本**：地图 Web 服务 API、天气/路况、AI 大模型推理费用。
- **研发与运维成本**：前后端研发、人机交互优化、监控告警、故障应急。
- **增长与渠道成本**：B 端销售/合作拓展、C 端投放与内容运营。

### 4.5 增长与渠道策略

- **C 端增长**：社交分享链路（房间码）、模板化场景（聚餐/看电影/团建）、内容种草（小红书/抖音/公众号）。
- **B 端增长**：以“团建/外勤集合/园区会议”作为切入口，提供可量化的效率提升报告与管理后台。
- **合作渠道**：与会议室预订、活动报名、出行服务平台做联合方案。

### 4.6 风险与对策（商业化视角）

- **第三方配额/稳定性风险**：引入缓存、限流、降级；关键接口监控与告警。
- **合规与隐私风险**：最小化收集位置数据；房间数据设置生命周期；提供显式告知与权限控制。
- **同质化竞争风险**：强化“房间码轻量协作 + 可解释 AI 决策”的差异化，并沉淀行业场景模板。

## 附录 术语表

|术语|说明|
|---|---|
|POI|兴趣点（Point of Interest），如餐厅、商场、公园等|
|地理编码|将地址文本转换为经纬度坐标的过程|
|加权中心点|根据参与者位置并按出行速度加权得到的地理中心点位，作为候选点搜索的中心|
|RESTful API|一种软件架构风格，用于设计网络应用程序的接口|
|WebSocket|一种在单个TCP连接上进行全双工通信的协议，支持实时数据传输|
|路径规划|根据出发地、目的地与出行方式，计算最优通行路径与时间的过程|
